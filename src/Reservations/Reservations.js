import React from 'react'
import './Reservations.css'

const Reservations = ({reservations}) => {
	return (
		<div className="reservations-container">I'm the reservatiosn container</div>
	)
}

export default Reservations